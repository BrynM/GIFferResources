@echo off

set FIX=a

if exist frames set FIX=frames\%FIX%

set DIRTOBE=%FIX%a
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%b
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%c
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%d
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%e
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%f
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%g
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%h
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%i
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%j
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%k
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%l
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%m
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%n
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%o
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%p
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%q
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%r
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%s
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%t
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%u
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%v
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%w
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%x
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%y
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

set DIRTOBE=%FIX%z
if not exist %DIRTOBE% mkdir %DIRTOBE% && echo %DIRTOBE%

pause